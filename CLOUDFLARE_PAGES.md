# Cloudflare Pages documentation pointer

The maintained deployment guide is [docs/deployment.md](docs/deployment.md).

This compatibility file is retained for existing bookmarks. It does not define
a separate deployment model.
