// Re-export shim — real implementation lives in @s-class/api/ApiError.
// Removed in Phase 5 when /src is decommissioned.
export { ApiError } from '@s-class/api/ApiError'
export type { ApiErrorBody } from '@s-class/api/ApiError'
